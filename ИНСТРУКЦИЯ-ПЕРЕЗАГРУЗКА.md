# 🔄 Перезагрузка Apache на HandyHost

## Способ 1: Через SSH (рекомендуется)

```bash
ssh u188054@109.95.210.183
cd www/zakup.one
chmod 644 index.html .htaccess
sudo systemctl restart apache2
```

Если команда `systemctl` не работает, попробуйте:

```bash
sudo service apache2 restart
# или
sudo /etc/init.d/apache2 restart
```

## Способ 2: Через панель управления HandyHost

1. Войдите в панель управления HandyHost
2. Найдите раздел "Веб-сервер" или "Web Server"
3. Найдите кнопку "Перезагрузить" или "Restart" для Apache
4. Нажмите её

## Способ 3: Использование скрипта

1. Загрузите файл `restart-apache.sh` на сервер
2. Подключитесь по SSH:
```bash
ssh u188054@109.95.210.183
chmod +x restart-apache.sh
./restart-apache.sh
```

## Проверка после перезагрузки

После перезагрузки Apache откройте в браузере:
- http://zakup.one
- http://zakup.one/index.html

Если все еще ошибка 403, проверьте:

1. **Права доступа:**
```bash
cd www/zakup.one
ls -la
# index.html должен иметь права 644
chmod 644 index.html .htaccess
```

2. **Проверьте что файлы на месте:**
```bash
cd www/zakup.one
ls -la index.html
# Должен показать файл
```

3. **Проверьте логи Apache:**
```bash
sudo tail -f /var/log/apache2/error.log
# или
sudo tail -f /var/log/httpd/error_log
```

## Если ничего не помогает

1. Убедитесь что файлы в правильной директории: `www/zakup.one/`
2. Проверьте настройки домена в панели HandyHost
3. Обратитесь в поддержку HandyHost

