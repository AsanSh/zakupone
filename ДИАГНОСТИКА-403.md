# 🔍 Диагностика ошибки 403 Forbidden

## Проблема: Все еще получаете 403 после загрузки файлов

Это означает проблему с расположением файлов или правами доступа.

## Шаг 1: Проверьте структуру директорий на сервере

Подключитесь по SSH или через файловый менеджер и проверьте:

```bash
ssh u188054@109.95.210.183
pwd  # Узнайте текущую директорию
ls -la  # Посмотрите что там
```

## Шаг 2: Найдите правильную директорию для сайта

На HandyHost обычно это одна из:
- `www/`
- `public_html/`
- `htdocs/`
- `www/zakup.one/`
- `public_html/zakup.one/`

**Важно:** Файлы должны быть в КОРНЕВОЙ директории домена, а не в подпапке!

## Шаг 3: Загрузите тестовый файл

1. Загрузите файл `test.html` в КОРЕНЬ директории сайта (не в подпапку!)
2. Откройте в браузере: `http://zakup.one/test.html`
3. Если открывается - значит проблема в расположении `index.html`

## Шаг 4: Правильное расположение файлов

Файлы должны быть так:

```
www/  (или public_html/)
├── index.html  ← ГЛАВНЫЙ ФАЙЛ ДОЛЖЕН БЫТЬ ЗДЕСЬ!
├── .htaccess
└── test.html
```

**НЕ так:**
```
www/
└── zakup.one/
    └── www/
        └── index.html  ← НЕПРАВИЛЬНО!
```

## Шаг 5: Исправьте права доступа

Через SSH:

```bash
cd www  # или public_html
chmod 644 index.html
chmod 644 .htaccess
chmod 755 .
```

## Шаг 6: Проверьте настройки домена в панели HandyHost

1. Войдите в панель управления HandyHost
2. Найдите раздел "Домены" или "Domains"
3. Проверьте настройки домена `zakup.one`:
   - **Корневая директория** должна быть: `www` или `public_html`
   - НЕ должна быть: `www/zakup.one` или `public_html/zakup.one`

## Шаг 7: Создайте index.html напрямую в корне

Если ничего не помогает, создайте файл `index.html` ПРЯМО в корневой директории через файловый менеджер:

1. Откройте файловый менеджер HandyHost
2. Перейдите в `www/` (или `public_html/`)
3. Создайте новый файл `index.html`
4. Скопируйте содержимое из `www/index.html` который я создал
5. Сохраните

## Шаг 8: Альтернатива - используйте другой файл

Если `index.html` не работает, попробуйте:

1. Переименуйте `index.html` в `default.html`
2. Или создайте `index.php` с содержимым:
```php
<?php header('Location: index.html'); ?>
```

## Быстрое решение

1. **Подключитесь по SSH:**
```bash
ssh u188054@109.95.210.183
```

2. **Перейдите в корневую директорию сайта:**
```bash
cd www  # или cd public_html
```

3. **Создайте index.html прямо там:**
```bash
cat > index.html << 'EOF'
<!DOCTYPE html>
<html>
<head>
    <title>ZAKUP.ONE</title>
</head>
<body>
    <h1>ZAKUP.ONE</h1>
    <p>Платформа снабжения строительных компаний</p>
</body>
</html>
EOF
```

4. **Установите права:**
```bash
chmod 644 index.html
```

5. **Проверьте:**
Откройте `http://zakup.one` - должно работать!

## Если все еще не работает

1. Проверьте логи ошибок в панели HandyHost
2. Убедитесь, что домен правильно настроен
3. Попробуйте открыть `http://109.95.210.183` - если работает, проблема в домене
4. Обратитесь в поддержку HandyHost с вопросом о правильной директории для домена

